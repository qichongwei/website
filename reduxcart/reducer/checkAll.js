/**
 * Created by qiwei on 2017/10/9.
 */
export  default  function counter(state = { count: 0 ,test:true}, action) {
    const count = state.count;
    switch (action.type) {
        case 'CheckAll':

        default:
            return state
    }
}